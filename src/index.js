document.addEventListener('DOMContentLoaded', function() {
  var level = document.querySelectorAll('.grid-row')
  let spriteNames = [
    "Croissant",
    "Cupcake",
    "Danish",
    "Donut",
    "Macaroon",
    "SugarCookie"]
  const columns =document.querySelectorAll('.grid-row')[0].getElementsByClassName('grid-cell').length
  const rows= document.querySelectorAll('.grid-row').length
  function init(){
    for(let i = 0; i < columns; i++){
      
    }
  }
}
